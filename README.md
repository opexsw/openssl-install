# openssl-install Cookbook

This cookbook installs openssl

### Platforms

- SandwichOS

### Chef

- Chef 12.0 or later

### Cookbooks

- `toaster` - openssl-install needs toaster to brown your bagel.

## Attributes

TODO: List your cookbook attributes here.

e.g.
### openssl-install::default

<table>
  <tr>
    <th>Key</th>
    <th>Type</th>
    <th>Description</th>
    <th>Default</th>
  </tr>
  <tr>
    <td><tt>['openssl-install']['bacon']</tt></td>
    <td>Boolean</td>
    <td>whether to include bacon</td>
    <td><tt>true</tt></td>
  </tr>
</table>

## Usage

### openssl-install::default

TODO: Write usage instructions for each cookbook.

e.g.
Just include `openssl-install` in your node's `run_list`:

```json
{
  "name":"my_node",
  "run_list": [
    "recipe[openssl-install]"
  ]
}
